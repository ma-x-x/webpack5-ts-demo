import styled from 'styled-components';

export const AppWrapper = styled.div`
  display: flex;
  flex-grow: 1;
  .app-main__wrapper {
    display: flex;
    flex-grow: 1;
    background: #fff;
  }
  .app_layout_foot {
    display: flex;
    justify-content: center;
  }
`;
